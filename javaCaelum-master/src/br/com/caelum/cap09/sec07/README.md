** DESAFIO: Não podemos dar new em Conta, mas por que, então, podemos dar new em Conta[10], por exemplo? **

Pode ser instanciado um ARRAY de contas pois esse ARRAY independe da implementação dos metodos abstratos, ele referencia objetos, esses objetos que devem ser instancias de uma classe que possui a implementação dos metodos abstratos; 